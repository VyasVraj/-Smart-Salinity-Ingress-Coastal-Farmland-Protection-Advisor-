# Backend API URL (leave empty to use Vite proxy in development)
# VITE_API_URL=http://localhost:5000

# Map provider: Leaflet + OpenStreetMap (free, no API key required)
# The Risk Heatmap works out of the box with no additional credentials.
